# AGENTS.md

## 项目规则

- 样式规则：样式以 Element Plus 默认和紧凑风格为准，实用性与功能性优先。
- 实现规则：实现应保持简单，采用 Rich Hickey 意义上的 simple：直接、清晰、少交织。非必要不抽象，不为了形式整洁而提取大量一次性辅助函数；这种做法应视为坏味道。只有在文件偏长、能减少大量重复代码、有助于理解结构，或涉及重要逻辑边界时，才考虑抽象。函数内部优先使用注释和必要的空行来区分逻辑段落，而不是依赖辅助函数切碎流程。目标是提升可理解性和阅读连贯性。
- 真相边界规则：每类数据只能有一个权威来源，系统内部只能消费该真相，禁止重新定义、修正，或通过 fallback（`||`、`??`、默认值）派生替代真相；真相无法构建时必须 fail fast。默认值仅在属于业务语义并被纳入真相定义本身时允许。
- 导出规则：如果一个模块为了外部使用而需要从 `index` 导出大量 schema、helper 或 internal type，应视为设计失败；优先收缩对外暴露面。
- TS 类型规则：TypeScript 类型同样适用真相边界规则、实现规则和导出规则；类型必须服务于真实约束和可理解性，否则就没有必要上类型。
- 中文优先规则：优先使用中文，尤其在注释、示例数据和说明文本中采用中文。
- Vue 样式规则：CSS 放在 Vue 单文件组件内；不在样式层面复用。需要复用时优先复用组件，同时保证各组件样式表现一致。
- 开发服务器规则：不必主动开启开发服务器，用户会自行开启。
- 最小覆盖规则：如无必要，不添加元素、文案、样式或占位结构；只实现当前需求所需的最小覆盖，后续需要时再补。
- Use Fowler's code smells as review vocabulary, but do not mechanically refactor all detected smells. Focus on smells introduced or worsened by the current change.
