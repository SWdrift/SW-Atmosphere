# Celestial System Memory

## 稳定架构

- `CelestialScenario + simulationTimeSeconds → CelestialSnapshot` 是天体运行时唯一入口。轨道和自转按当前时间直接求值，不积累逐帧位置。
- 系统空间在 CPU 使用 `number` 和 km；相机绑定只派生导航参考系。上传 GPU 前先在 CPU 做系统位置相减，GPU 只消费相机相对 `f32` 中心/半径。
- 相机绑定由参考天体和 `inertial` / `body-fixed` 参考系组成；切换绑定保持系统空间相机姿态，不改变天体快照或大气宿主。
- 太阳、地球、月球尺度属于天体场景；月面反射率属于独立材质。大气静态参数不得重新保存太阳角半径或月球距离。

## 食相与缓存边界

- 照明遮挡使用观察点处太阳/月球视圆的解析交叠面积；可见太阳图像则按逐像素月球球体覆盖裁切，二者不能互相替代。
- WGSL 的圆盘抗锯齿使用 `fwidth` 时，导数必须在 `fs_main` 的统一控制流中求值，再把覆盖率传入逐像素命中分支；不得在月球球体命中后的非一致分支中间接调用导数。
- 日食可能扫过地球时，最终画面强制使用逐像素积分。Sky-View 和 Aerial Perspective 仍是轴对称调试近似，不能作为月影真值。
- 当前地球大气参数在 renderer 创建时静态上传，因此地球实体半径必须与大气底部半径相等并在场景边界 fail fast；太阳和月球实体半径可直接调节。

## 场景语义

- 默认场景是可重复的北半球冬季朔月日食调试场景，不代表某个真实历史时刻。精准事件应通过外部星历生成同一种场景参数、历元和模拟起始时间，不建立第二套运行路径。
- 验证构图若要求月球落在指定画面位置，应先用相机射线与月球轨道球求交，再将所得地心方向反解为轨道初相；禁止保存验证专属月球世界位置或屏幕位置。
- 旧验证用例的当地太阳高度只作为场景生成输入：它被转换成确定性地球轨道初相，运行时仍只消费天体快照。
- CPU 近似 Kepler 求值的成本远低于当前 GPU 大气积分；性能治理应优先关注日食逐像素路径和 LUT 重建，而不是把轨道求值迁移到 GPU。
