# Celestial System Control Plane

## Control Plane Role

本控制平面约束行星大气实验中的太阳—行星—卫星实体、近似轨道、自转、模拟时间、参考天体和 CPU→GPU 帧边界。它把原先分散的太阳方向、月球方向和地心固定世界统一为一份可复现的天体状态，为昼夜、季节、月相、日食及后续其他星球场景提供几何真相。

首期固定三体拓扑：太阳是系统根实体，地球绕太阳，月球绕地球。不计算引力和天体间动力学反馈；轨道与自转只由历元参数和模拟时间确定性求值。实体参数允许编辑以构建其他恒星—行星—卫星组合，但暂不建立任意增删天体编辑器。

## Meta Reference

- 先读 `cp-meta.md`、`document/行星大气与动态天气长期目标.md`、`document/行星大气光学与视象图谱.md` 和 `document/地球占比和轨道高度估计.md`，再读本文件。
- 大气渲染读取 `cp-atmosphere.md`；摄像机控制和参考天体读取 `cp-atmosphere-camera.md`；视觉与食相验收读取 `cp-atmosphere-visual.md`；固定事件和 URL 验证读取 `cp-atmosphere-workbench.md`。
- 月球既有边界读取 `cpm-atmosphere-moon.md`；本 scope 的稳定结论沉淀到 `cpm-celestial-system.md`。
- 修改或审查代码时使用 `code-smell-guard`；阶段完成或动态任务区膨胀时使用 `compact-control-plane`。

## Scope

- 主作用域：`apps/examples/src/pages/planetary-atmosphere/` 内的天体定义、轨道/自转求解、模拟时钟、场景状态、参考系转换、GPU 帧数据、天体控制 UI、食相几何及测试与 README。
- 权威入口：`CelestialScenario + simulationTimeSeconds → CelestialSnapshot`；任何系统位置、姿态、太阳方向、月球方向、季节和食相都只能从该快照派生。
- 直接调用方：`AtmosphereScene`、摄像机控制适配、`AtmosphereRenderer`、验证用例、天体/调试面板和遥测。
- 最小公共面：创建/校验场景、按时间求值快照、构建参考空间与 GPU 帧、更新可调轨道/自转参数；不暴露求解器内部临时量或建立大 barrel export。
- 首期外：N-body 引力、摄动、高精度长期历表传播、任意天体增删、月面/太阳表面纹理、潮汐与地形遮挡。

## Core Rules

### 时间、轨道与实体真相

- 轨道位置和自转姿态只由参数、历元和当前模拟时间直接求值；禁止逐帧积分累计位置或同时保存可独立修改的实时位置。
- 系统空间使用 CPU JavaScript `number`、km、右手 Z-up；太阳固定为系统根，地球和月球按父子轨道递归派生系统位置。
- 近似轨道使用半长轴、偏心率、倾角、升交点经度、近心点幅角、历元平均近点角和周期；参数非法或父子关系无法构建时 fail fast。
- 自转使用世界系统中的极轴、周期和历元相位；表面参考系只能由同一自转定义派生。
- 太阳、地球和月球都是有半径和材质/辐射参数的实体。太阳是有限发光球体，不再以无限远单位方向作为场景真相。

### 参考系与摄像机

- 系统空间是轨道权威；参考空间是派生的导航/调试空间，默认地心原点。切换参考天体不得修改天体状态。
- 摄像机绑定显式表示为 `referenceBodyId + referenceFrame`；`body-fixed` 跟随天体平移和自转，`inertial` 只跟随天体平移。
- 重新绑定默认保持摄像机系统空间位置和姿态；跳转到参考天体默认视角必须是独立操作。
- 摄像机参考天体与大气宿主是两个独立概念，不得因绑定月球而把大气宿主切换为月球。
- GPU 始终以摄像机为数值原点；所有系统空间相减在 CPU 双精度完成后再转换为 f32。变量和类型必须明确区分 system、reference、camera-relative。

### GPU、遮挡与大气

- GPU 帧必须提供太阳、地球和月球相对摄像机的实体中心/半径，以及相对当前大气宿主的照明几何；shader 不消费 UI 轨道参数，也不重复求轨道。
- 可见太阳、月球和地球由真实球体或等价解析投影产生；不得使用固定角尺寸、二维贴片或隐藏放大。
- 每个地表和大气采样点的太阳输入由有限太阳圆盘同时扣除地球地平线和月球圆盘遮挡；同一可见率进入地表直射、Reference、Production 单次散射和可支持的多重散射输入。
- 月球反射使用唯一太阳实体照明；背景天体仍按 `backgroundRadiance × transmittance + inScatteredRadiance` 合成。
- 日食破坏球对称 LUT 假设时必须显式隔离近似：首期高标准路径以逐像素/逐采样解析遮挡为权威，不能把未表达月影的全局 LUT 宣称为食相真值。

### UI、验证与迁移

- 天体、输出和调试控制必须是独立组件/面板；天体面板管理模拟时间、实体选择、轨道和自转，输出面板管理质量/散射/曝光，调试面板管理 debug view、网格、指示器和遥测。
- UI 使用便于调试的轨道分组和单位，但只编辑权威场景参数；太阳/月球方位角和高度角在迁移完成后删除，不保留兼容 fallback。
- 固定事件是完整 `CelestialScenario + simulationTime + observer/camera` 快照；精准来源与手工调试只负责生成同一种初始状态，运行时不分叉。
- CPU 测试覆盖圆/椭圆轨道、父子组合、历元重现、自转、参考系切换、季节、朔望和日食几何；运行 `pnpm check:quick`、回归测试和生产构建。

## Task Board

- [ ] 在用户启动的 WebGPU 页面完成有限日月圆盘、绑定切换和月影的视觉验收。
