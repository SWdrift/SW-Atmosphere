# Atmosphere Iteration Workbench Control Plane

## Control Plane Role

本控制平面约束大气页面的迭代与验证工作台。它把参考图对比、可复现画面、路径化画面操作、URL 深链接和自动化验证组织为同一套可执行流程，减少依赖人工寻找位置、重复调参和记录上下文。

本平面是 `cp-atmosphere.md` 的下位工程平面，与 `cp-atmosphere-visual.md`、`cp-atmosphere-performance.md` 协作，但不重新定义视觉真实性、物理正确性或性能通过标准。参考图和操作脚本只是稳定地构建验证上下文；“Best” 必须由命中的上位或专项 CP 的证据共同判定，不能由截图相似度或按钮激活状态直接宣称。

## Meta Reference

- 先读根目录 `cp-meta.md`、`cp-atmosphere.md`，再读本文件；具体任务同时读取命中的视觉、性能或天气 CP。
- 可复用大气工程记忆读取 `cpm-atmosphere.md`；摄像机路径边界读取 `cp-atmosphere-camera.md`；本 scope 形成稳定工程结论后沉淀到 `cpm-atmosphere-workbench.md`。
- 同时遵循根目录 `AGENTS.md`、`apps/examples/AGENTS.md` 与 `apps/examples/src/pages/AGENTS.md`。
- 修改或审查代码时使用 `code-smell-guard`；阶段完成或动态任务区膨胀时使用 `compact-control-plane`。

## Scope

- 控制对象：大气实验页内的验证用例、参考图展示、确定性场景激活、动作路径、集中状态、内部操作 API、URL 同步和相关测试。
- 当前入口点：大气实验页面、预设面板、页面路由、状态容器、场景协调层、摄像机控制和固定场景定义；目录或文件可随重构变化，本平面约束职责而非现有文件名。
- 直接调用方：页面与面板 UI、路由适配层、人工验证流程，以及后续浏览器或截图自动化。
- 最小公共 API：按稳定 ID 激活/取消验证用例、执行/停止动作路径、调整独立参考图设置、读取当前工作台快照；渲染器、摄像机和参数内部对象不直接暴露给 UI 或测试。
- 允许配套改动：验证资源元数据、参考图片资产、路由、页面 README、确定性 CPU 测试和必要的浏览器验证入口。
- 阶段外：修改大气物理模型来追图、用参考图替代数值基准、通用工作流引擎、通用状态框架、录制任意用户操作，以及与当前验证无关的页面重构。

## Core Rules

### 用例与真相边界

- “摄像机预设”只表达单次相机定位；“验证用例”表达可复现的完整验证上下文。需要参考图、URL、参数或动作路径时必须使用验证用例，不继续扩张摄像机预设的职责。
- 每个验证用例由一个稳定 ID 和一份权威定义构成；标签、初始相机状态、渲染参数、太阳状态、可选参考图、动作路径和 URL 均消费该定义，不在组件、路由或测试中复制映射。
- 验证用例必须显式给出构建目标画面所需的全部状态。必需数据缺失、ID 未知、动作无法执行或参考资源无效时 fail fast，不读取当前页面残留状态，不用 fallback 拼出近似场景。
- 并非所有验证用例都有参考图；用例必须以参考元数据或显式 `null` 表达，不得 fallback 到其他用例的图片。有参考图时，其图片源、裁切/适配方式、可比较内容、不可确认变量和来源说明属于用例元数据；不得由参考图反推并覆盖物理参数。

### 集中状态与操作 API

- 工作台状态只有一个权威容器，至少明确表达当前验证用例、执行中的动作路径、参考图显示状态、混合比例和执行阶段。组件只读取状态并调用 action，不直接拼装跨模块状态变化。
- 参考图显示状态与混合比例属于独立调试状态，默认分别为不显示和 `0.5`；激活、切换或取消验证用例不得重置它们。当前用例没有参考图时不渲染图片，但仍保留该设置。
- 相机姿态、太阳、FOV、曝光、质量和调试视图各自仍只能有一个运行时真相。集中管理不是复制这些状态，而是由工作台 action 按确定顺序命令其权威拥有者，并从同一入口生成可验证快照。
- 页面、路由、按钮和测试共同消费最小命令面，优先为：
  - `activateCase(id)`：原子地构建用例初始状态，成功后才标记激活。
  - `deactivateCase()`：停止用例路径并清除激活状态，不擅自恢复业务参数或独立参考图设置。
  - `runPath(id)` / `stopPath()`：执行或停止当前用例声明的动作路径。
  - `setReferenceVisible(visible)` / `setReferenceMix(mix)`：更新独立参考图设置并同步 URL。
  - `getSnapshot()`：返回断言所需的只读、可序列化状态，不暴露可变内部对象。
- API 名称可在实现时按当前结构最小调整，但上述语义、单向调用关系和 fail-fast 边界不得分散到组件事件链中。

### 动作路径

- 动作路径是验证用例内的确定性声明，不是任意回调集合。第一版只支持当前实验确实需要的少量命令：设置参数、设置绝对相机姿态、按明确时长移动/转动、等待稳定条件和建立检查点。
- 每条路径必须从用例定义的完整初始状态开始；同一输入、固定画布和相同设备条件下应得到相同的命令序列与检查点。
- 位置和姿态优先使用绝对世界空间目标；连续移动明确起点、终点、持续时间和插值语义。不得依赖帧率累计键鼠输入来定义可复现路径。
- 参数变化必须调用其权威 action；路径执行器只编排命令，不重新实现相机数学、参数校验或渲染 dirty dependency。
- 人工输入与路径并发时必须有确定语义。第一版路径执行期间禁止相互冲突的人工画面操作；停止路径后再恢复人工控制。

### URL 与激活生命周期

- 验证用例使用可复制、可前进后退的稳定 URL，目标形态为 `/planetary-atmosphere/presets/:caseId`；面板基础路由仍为 `/planetary-atmosphere/presets`。
- 参考图显示状态和混合比例分别使用 `reference=0|1` 与 `mix=0..1` 查询参数，并在大气页面标签切换时保留；缺失时使用其业务默认值，非法值 fail fast。
- 点击验证用例按钮先导航到该 URL，再由路由状态驱动统一的激活入口；直接访问、刷新、浏览器前进后退和按钮点击不得形成两条应用逻辑。
- 页面场景尚未就绪时，路由请求保持为待激活状态；场景就绪后只应用一次。未知 `caseId` 必须产生明确错误或受控的未找到结果，不静默选择首项。
- 只有验证用例 URL 对应的按钮显示激活。进入另一个用例 URL 时原子切换；离开“预设”标签页或离开大气页面时调用 `deactivateCase()`。
- 用例激活表示当前验证会话归属于该用例，不等于画面始终与初始快照逐字段相等；允许激活后按用例目的调参或运行路径。需要判断画面是否偏离时使用显式快照或 dirty 状态，不以按钮样式冒充一致性证明。

### 对比与验证

- 有参考图的验证用例在独立显示开关开启时，于画布工作区提供覆盖对比及混合比例控制；切换用例不隐式开启参考图。
- 参考图和渲染画面必须保持各自原始宽高比；若构图、FOV 或裁切不同，应明确标示，不用非等比拉伸制造相似。
- 每个用例至少可断言：路由与激活 ID 一致、完整初始状态已应用、可选参考资源语义正确、动作路径到达检查点、离开预设页后已取消激活；参考图设置在用例切换中保持独立。
- CPU 测试覆盖 ID 解析、状态转换、命令顺序和 URL 生命周期；真实 WebGPU 画面继续通过固定用例、专项 debug、截图/像素证据和人工观感验证。
- 自动化输出必须记录用例 ID、画布物理尺寸、DPR、设备/浏览器、相机快照、参数快照和专项 CP 要求的测量信息；缺失关键上下文时不得比较或宣称通过。

## Task Board

- [ ] 在允许操作本地页面时，人工复查真实 WebGPU 画面的参考层、用例切换、动作路径、前进后退和离开标签页取消语义；不得用 CPU 测试或构建结果替代观感结论。
